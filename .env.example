# Mux is used to serve reel clips + auto-generate poster frames.
# Create an asset in the Mux dashboard, copy its Playback ID into
# src/content/projects.ts (muxPlaybackId). No key is needed at build time
# for public playback IDs. Signed playback would require these:
# MUX_TOKEN_ID=
# MUX_TOKEN_SECRET=
